#!/usr/bin/python3
def number_keys(a_dictionary):
    if a_dictionary:
        for number_of_keys, key in enumerate(a_dictionary):
            continue
        return number_of_keys + 1
    else:
        return 0
