#!/usr/bin/python3
def multiple_returns(sentence):
    if sentence != "":
        for count, character in enumerate(sentence):
            continue
    else:
        return ((0, None))
    return((count + 1, sentence[0]))
